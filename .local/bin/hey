#!/bin/bash
if [ "$1" == "" ]
then
	echo "dude are you retarded"
	exit 69
else
	if [ "$2" == "" ]
	then
		sudo "$1"
	else
		if [ "$3" == "" ]
		then
			sudo "$1" "$2"
		else
			sudo "$1" "$2" "$3"
		fi
        fi
fi
